// import { merge_sort } from './merge_sort';

describe('merge sort', () => {
  test('test 1', () => {
    const _a = [2, 5, 3, 8, 16, 9, 26, 89, 4];
    // merge_sort(_a);
    // expect(merge_sort(_a)).toBe([2, 3, 4, 5, 8, 9, 16, 26, 89]);
  });
});
